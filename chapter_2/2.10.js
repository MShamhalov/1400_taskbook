// Tags: prompt, console.log, Math.pow() (**), шаблонная строка (${}), Math.fixed()

const prompt = require('prompt-sync')();

console.log('Поиск среднего арифметическо и среднего геометрического двух целых чисел');
